import ResetPasswordClient from "./ResetPasswordClient";

export const metadata = {
  title: "Reset password - Opentip",
};

export default function ResetPasswordPage() {
  return <ResetPasswordClient />;
}
